# moxywolf-fetch-viewer

Minimal tree view of Linkup `/fetch` structured output. Same collapsible outline as Moxywolf’s hierarchy HTML — no PDF overlay.

```bash
npm start
```

Open http://127.0.0.1:3344. Sample PMF tags load immediately. Paste a Linkup API key and a public PDF URL to re-fetch.
